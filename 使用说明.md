# 🎤 校园十佳歌手 · 实时投票系统 使用说明

一个**零依赖**的局域网实时投票系统：学生手机扫码投票，老师大屏实时看排名，自带演示模式。

---

## 一、快速开始（30 秒）

1. **双击 `启动投票系统.bat`**（或命令行运行 `node server.js`）
2. 启动后窗口会显示本机局域网地址，并自动打开**管理后台**
3. 三个页面：

| 页面 | 地址 | 用途 |
|------|------|------|
| 投票页 | `http://本机IP:3000/` | 学生手机投票 |
| 实时大屏 | `http://本机IP:3000/screen` | 投影/大屏实时排名 |
| 管理后台 | `http://本机IP:3000/admin` | 你和老师操作（默认密码 `123456`） |

> 本机演示直接用 `http://localhost:3000/admin`；学生手机访问要用启动窗口里显示的**局域网地址**（形如 `http://192.168.x.x:3000/`），且手机和电脑必须连**同一个 WiFi**。

## 二、给老师演示的流程

1. 双击启动，打开管理后台（`/admin`）
2. 打开**实时大屏**（`/screen`），点右上角「⛶ 全屏」，投到投影仪
3. 在后台「控制台」点击 **▶ 投票中**
4. 点击 **「开启模拟投票」**（可选速度）——大屏排名实时跳动、总票数飙升、最新投票滚动，效果直接拉满 🎉
5. 同时可以用手机打开投票页，真实投几票，能看到票数**全端同步**
6. 演示完点「⏹ 停止模拟投票」，再点 **「🗑 清空所有票数」**，即可干净地开始正式使用

> ⚠️ 注意：模拟产生的票数会计入数据。正式比赛前务必先「清空所有票数」。

## 三、正式比赛流程

1. **赛前准备**（管理后台）：
   - 「选手管理」：把示例选手改成真实名单（添加/编辑/删除，可上传照片）
   - 「系统设置」：改活动标题、每人可投票数（默认 3 票）、是否允许重复投同一选手
   - 「系统设置」：修改管理密码（默认 `123456`，建议修改）
   - 点「🗑 清空所有票数」，确保从零开始
2. **比赛中**：
   - 状态切到 **▶ 投票中**，学生扫码投票
   - 大屏实时显示排名：**前 10 名进入绿色「晋级区」**，第 1-3 名有金银铜牌，被反超时排名自动动画换位
   - 需要暂停时就切回 **⏸ 未开始**；结束时切到 **🏁 已结束**（投票页会提示结束，但仍可看结果）
3. **赛后**：控制台点 **「⬇ 导出结果 CSV」**，用 Excel 打开即为最终排名（前 10 名即"校园十佳歌手"）

## 四、常见问题

**Q：学生手机打不开投票页？**
- 确认手机和电脑连同一个 WiFi
- 第一次启动时 Windows 防火墙可能弹窗，请点 **「允许访问」**（需勾选"专用网络"）
- 如果还是不行：控制面板 → Windows Defender 防火墙 → 高级设置 → 入站规则 → 新建规则，放行 TCP 3000 端口
- 确认用的是启动窗口里显示的局域网地址（192.168.x.x），不是 localhost

**Q：想先灌一批测试数据看效果？** 服务启动后命令行运行 `node test-data.js`，会模拟约 680 名观众投出约 1400 票（分布有热门/冷门梯度），并自动开启投票。清除方式同上：后台点「清空所有票数」。

**Q：想换端口？** 用命令行启动：`set PORT=8080 && node server.js`

**Q：数据会丢吗？** 不会。所有数据实时保存在 `data/data.json`，重启电脑也不丢。想彻底重置就删掉这个文件再重启。

**Q：防刷票原理？** 每台设备浏览器有唯一标记，投完限定票数就不能再投（学生清除浏览器数据可重置，局域网场景足够用）；同一 IP 每分钟限 1200 票，防脚本刷票。

**Q：一场比赛多个环节（如初赛/决赛）？** 每个环节开始前点一次「清空所有票数」即可。

## 五、微信扫码 / 学生用手机流量投票（外网访问）

默认模式是「局域网」：学生必须连学校同一个 WiFi。如果学生**不在同一 WiFi**（各自用手机流量），需要做一次**内网穿透**，让公网也能访问投票页。全程约 10 分钟，免费即可。

### 步骤（以 natapp 为例）

1. 打开 **natapp.cn**，注册账号并完成实名认证
2. 购买一条「**免费隧道**」：协议选 `web`，本地端口填 `3000`
3. 在官网「我的隧道」页复制 **authtoken**，下载客户端 natapp.exe 放到任意文件夹
4. 在该文件夹打开命令行，运行：`natapp -autotoken=你的authtoken`（注意替换成自己的）
5. 窗口会显示一个公网地址（形如 `http://xxxx.natapp1.cc` / `https://...`）
6. 打开**管理后台 → 「外网投票」卡片**，把这个地址粘贴进去，点「保存」
7. 大屏和后台的二维码**立刻变成公网版**——学生微信扫码、用手机流量都能直接进投票页

> cpolar 同理：注册 cpolar.com → 后台获取隧道 token → 运行 `cpolar http 3000` → 得到 https 地址 → 粘贴到管理后台。

### 注意事项

- 免费版公网地址在**穿透工具重启后会变化**，变了到后台重新粘贴一次即可
- 比赛期间请保持：电脑开机、本系统运行、穿透工具运行
- 公网环境下**务必把管理密码改复杂**（后台「系统设置」里改），地址只发给学生群
- 活动结束后关闭穿透工具、在后台「清除」外网地址，即回到纯局域网模式

## 六、云服务器部署（500 人规模推荐）

预计观众超过一两百人、或学生大量使用手机流量时，推荐把系统部署到**云服务器**上：不需要你的电脑开机，没有穿透隧道这一层，带宽稳定。

### 部署步骤（Ubuntu/Debian，约 15 分钟）

1. 租一台**轻量应用服务器**（阿里云/腾讯云新用户优惠，最低配 2核2G 足够），系统选 Ubuntu 22.04/24.04，记下**公网 IP**
2. 在云控制台「防火墙/安全组」**放行 TCP 8010 端口**（部署脚本默认用 8010，避免和服务器上其它项目冲突）
3. 用 WinSCP / 宝塔面板把整个项目文件夹上传到服务器，例如 `/root/toupiao`
4. SSH 登录服务器（可用 Windows 自带的 `ssh root@服务器IP` 或 PuTTY），执行：
   ```bash
   cd /root/toupiao && bash server-setup.sh
   ```
   脚本会自动：安装 Node.js → 复制到 /opt/vote → 注册 systemd 开机自启 → 启用云模式（自动识别公网 IP 用于二维码）
5. 打开 `http://服务器公网IP:8010/admin` 验证，**修改管理密码**，完成

之后大屏、后台、学生投票全部走 `http://服务器公网IP:8010`，与任何人的本地网络无关。

### 日常运维

- 系统已注册 systemd 服务：崩溃自动重启、服务器重启自动拉起
- 常用命令：`systemctl restart vote`（重启）、`systemctl status vote`（状态）、`journalctl -u vote -f`（看日志）
- 升级代码：覆盖 `/opt/vote` 下的 `server.js` 和 `public/` 后再执行 `systemctl restart vote`（**票数数据在 `/opt/vote/data/`，不会被覆盖丢失**）
- 赛后：后台导出 CSV 留存；下一年在后台「清空所有票数」+ 更新选手名单即可复用

## 七、文件结构

```
实时投票系统/
├── server.js           # 全部后端逻辑（零依赖，Node 原生模块）
├── 启动投票系统.bat     # Windows 双击启动（本机/局域网用）
├── server-setup.sh     # 云服务器一键部署脚本（Ubuntu/Debian）
├── 使用说明.md          # 本文件
├── e2e-test.js         # 58 项自动化测试（node e2e-test.js）
├── test-data.js        # 一键灌入演示数据（node test-data.js）
├── data/               # 运行数据（自动生成）
└── public/
    ├── vote.html       # 投票页（手机端）
    ├── screen.html     # 实时大屏
    ├── admin.html      # 管理后台
    └── js/ css/        # 前端代码与本地二维码库
```
